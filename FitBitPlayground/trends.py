import json, pathlib

def detect_wakeup(id):
    """
    Detect naps from steps patterns

    :id intraday series from fitbit
    :returns: boolean, True if we just got up from a nap/bed.
    """
    pass